const Actions = {
    IN: "in",
    OUT: "out",
};

const LogTypes = {
    REMOTE: "remote",
    BIOMETRIC: "biometric",
    RFID: "rfid",
};

const AttendanceLogsConstants = { Actions, LogTypes };
export default AttendanceLogsConstants;
